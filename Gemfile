source 'https://rubygems.org'

# Use Cinch fork directly from GitHub (same as ZbojeiJureq)
gem 'cinch', git: 'https://github.com/blolol/cinch.git'

# Extracted standard libraries required by Ruby 4
gem 'base64'
gem 'logger'
gem 'minitest', '~> 5.25'
gem 'ostruct'

group :development do
  gem 'mutant', '~> 0.16'
  gem 'mutant-minitest'
end
